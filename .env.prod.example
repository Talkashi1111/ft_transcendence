# Production Environment Configuration for ft_transcendence
# Copy this file to .env.prod and set your values before running:
#   docker compose -f docker-compose.prod.yml --env-file .env.prod up
#
# DEFENSE SETUP:
# 1. Find server IP: ip addr | grep inet
# 2. On each client, add to /etc/hosts: <server-ip> mooo.com
# 3. Access via: https://mooo.com

# ======================
# Secrets (REQUIRED)
# ======================
# JWT signing secret - MUST be set!
# Generate with: openssl rand -hex 32
JWT_SECRET=

# ======================
# OAuth Configuration (Optional - for Google Login)
# ======================
# Google OAuth credentials from Google Cloud Console
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=411572592768-8mkk0bpi9vc8q488v6b5j6bfobcejbr3.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=

# OAuth callback URL - uses mooo.com domain
# Register this URL in Google Cloud Console as authorized redirect URI
OAUTH_CALLBACK_URI=https://mooo.com/api/oauth/google/callback

# ======================
# Two-Factor Authentication (2FA) (Optional)
# ======================
# Encryption key for TOTP secrets (32 bytes = 64 hex characters)
# Generate with: openssl rand -hex 32
TWO_FACTOR_ENCRYPTION_KEY=

# ======================
# Blockchain Configuration (Optional)
# ======================
# Private key for deploying/interacting with smart contracts
# WARNING: Never commit real private keys!
PRIVATE_KEY=

# Avalanche Fuji testnet RPC URL
FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

# Deployed TournamentScores contract address
CONTRACT_ADDRESS=0xc673e53845eb89Ab38166F8ACbAc92e0EB7a973D
