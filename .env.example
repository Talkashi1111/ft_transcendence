# Environment Configuration for ft_transcendence
# Copy this file to .env and adjust values as needed

# ======================
# Backend Configuration
# ======================
# Port the backend server listens on (inside container or devcontainer)
PORT=3000

# Host interface to bind to (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
NODE_HOST=0.0.0.0

# Environment mode: development | production | test
NODE_ENV=development

# Host port for production deployment (used in logging)
# This should match the host port in docker-compose.prod.yml
HOST_PORT=8080
