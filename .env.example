# Environment Configuration for ft_transcendence (Development)
# Copy this file to .env and adjust values as needed

# NOTE: For production, see .env.prod.example
# Backend and blockchain have their own .env files:
# - backend/.env.example - Server and blockchain integration config
# - blockchain/.env.example - Smart contract deployment config

# ======================
# Docker Configuration
# ======================
# Port the backend server listens on (inside container or devcontainer)
PORT=3000

# Host interface to bind to (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
NODE_HOST=0.0.0.0

# Environment mode: development | production | test
NODE_ENV=development
