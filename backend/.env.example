# Backend Environment Configuration

# ======================
# Server Configuration
# ======================
# Port the backend server listens on (inside container or devcontainer)
PORT=3000

# Host interface to bind to (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
NODE_HOST=0.0.0.0

# Environment mode: development | production | test
NODE_ENV=development

# Host port for production deployment (used in logging)
# This should match the host port in docker-compose.prod.yml
HOST_PORT=8080

# ======================
# Blockchain Configuration
# ======================
# Private key for blockchain transactions (without 0x prefix)
PRIVATE_KEY=your_private_key_here

# Avalanche Fuji Testnet RPC URL
FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

# Deployed contract address (set after deploying TournamentScores contract)
CONTRACT_ADDRESS=0xc673e53845eb89Ab38166F8ACbAc92e0EB7a973D

# ======================
# Database Configuration (Prisma)
# ======================
# SQLite database file path (absolute path inside container)
# See: https://www.prisma.io/docs/orm/overview/databases/sqlite
DATABASE_URL="file:/app/data/database.db"

# ======================
# JWT Authentication
# ======================
# Secret key for signing JWT tokens (CHANGE THIS IN PRODUCTION!)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# ======================
# OAuth Configuration
# ======================
# Google OAuth Client ID (obtain from Google Cloud Console)
GOOGLE_CLIENT_ID=

# Google OAuth Client Secret (obtain from Google Cloud Console)
GOOGLE_CLIENT_SECRET=

# NOTE: OAUTH_CALLBACK_URI is no longer needed!
# The backend dynamically builds callback URI from the request host.
# Register ALL these URIs in Google Cloud Console:
#   - http://localhost:5173/api/oauth/google/callback (dev)
#   - https://localhost/api/oauth/google/callback (prod local)
#   - https://mooo.com/api/oauth/google/callback (prod remote)

# Additional allowed hosts for OAuth (comma-separated, optional)
# Use this when accessing from custom domains or IP addresses
# Example: OAUTH_ALLOWED_HOSTS=mycompany.com,192.168.1.100
# OAUTH_ALLOWED_HOSTS=

# ======================
# Two-Factor Authentication (2FA)
# ======================
# Encryption key for TOTP secrets (32 bytes = 64 hex characters)
# Generate with: openssl rand -hex 32
TWO_FACTOR_ENCRYPTION_KEY=your_64_char_hex_key_here_generate_with_openssl_rand_hex_32_cmd

